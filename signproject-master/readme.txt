First, set the mysql
__________________________________________________________
create database EM;
use EM;
create table menu (name varchar(30), price float, store varchar(30));
__________________________________________________________
